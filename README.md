# python wrapped qdel command
This command delete jobs that are accidentally alive after `qdel` in computarional nodes of a cluster system. This can be used instead of `qdel`.

Usage
```
pyqdel [jobid]
```